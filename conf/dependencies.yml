# Application dependencies

require:
    - play
    - provided -> twitter4j-core 2.2.5
repositories:
    - provided:
            type: local
            artifact:   "${application.path}/jar/[module]-[revision].jar"
            contains:
            - provided -> *     